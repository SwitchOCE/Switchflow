interface:
  display_name: "Intake"
  short_description: "Start or resume durable scope discovery"
  default_prompt: "Use $intake to start or resume scope discovery for this {{PROJECT_NAME_YAML_DOUBLE}} milestone."

policy:
  allow_implicit_invocation: false
